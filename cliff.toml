# git-cliff configuration for muxr-core releases.
# Drives both the auto-computed version bump (`git cliff --bumped-version`) and
# the per-release changelog body in .github/workflows/release.yml.

[changelog]
header = """
# Changelog

All notable changes to muxr-core (muxrd + muxrctl) are documented here.\n
"""
body = """
{% if version %}\
## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
## [Unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
### {{ group | striptags | trim | upper_first }}
{% for commit in commits %}\
- {% if commit.scope %}*({{ commit.scope }})* {% endif %}\
{% if commit.breaking %}[**breaking**] {% endif %}\
{{ commit.message | split(pat="\n") | first | upper_first }}
{% endfor %}
{% endfor %}\n
"""
footer = ""
trim = true

[git]
conventional_commits = true
filter_unconventional = false
split_commits = false
commit_parsers = [
    { message = "^feat", group = "Features" },
    { message = "^fix", group = "Bug Fixes" },
    { message = "^doc", group = "Documentation" },
    { message = "^perf", group = "Performance" },
    { message = "^refactor", group = "Refactoring" },
    { message = "^style", group = "Styling" },
    { message = "^test", group = "Testing" },
    { message = "^chore\\(release\\)", skip = true },
    { message = "^chore\\(deps\\)", skip = true },
    { message = "^chore|^ci", group = "Miscellaneous" },
    { body = ".*security", group = "Security" },
    { message = "^revert", group = "Reverted" },
    { message = "^WIP", skip = true },
    { message = "^index on", skip = true },
    { message = "^Merge branch", skip = true },
    { message = ".*", group = "Other Changes" },
]
protect_breaking_commits = false
filter_commits = false
# Anchored so the existing non-semver `fonts-v1` tag (font catalog release) is
# NOT treated as a release baseline — only `vX.Y.Z` tags drive versioning.
tag_pattern = "^v[0-9].*"
sort_commits = "oldest"

[bump]
initial_tag = "v0.1.0"
